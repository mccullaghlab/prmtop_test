
#include <stdio.h>
#include <stdlib.h>

#define nDim 3

class config
{
	private:
		int i;
		FILE *configFile;
	public:
		float T;    // temperature
		float dt;   // integration timestep
		float pnu;  // Anderson thermostat frequency	
		int nSteps;
		int deltaWrite;
		int deltaNN;
		float lbox;
		char *prmtopFileName;
		// initialize all variables
		//void initialize();
		
};
